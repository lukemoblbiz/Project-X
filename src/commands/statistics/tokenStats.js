module.exports = {
    name: 'tokenstats',
    description: 'Check the statistics and information of a token.',
    devOnly: true,
    testOnly: true,

    callback: async (client, interaction) => {

    }
}