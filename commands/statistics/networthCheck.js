module.exports = {
    name: 'checknetworth',
    description: 'Calculate the networth of a wallet address.',
    devOnly: true,
    testOnly: true,

    callback: async (client, interaction) => {

    }
}